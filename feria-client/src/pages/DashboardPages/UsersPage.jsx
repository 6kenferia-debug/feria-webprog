import React from 'react';
import { Box } from '@mui/material';

const UsersPage = () => {
    return (
        <Box sx={{
            minHeight: '100vh',
            px: { xs: 1, md: 2 },
            py: { xs: 2, md: 2 },
            backgroundColor: '#eef4f7',
        }}>
        </Box>
    );
};

export default UsersPage;
